"""Operational safety: kill-switch control and alerting.

A human-operated kill switch (the safe direction — it can only halt, never widen
authority) and a structured alert sink for drawdown halts and model rollbacks.
"""
